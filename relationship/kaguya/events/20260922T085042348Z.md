---
at: 2026-09-22T08:50:42.348Z
persona: calli
target: kaguya
source: live
axis_deltas:
  trust: 0.5
  respect: 0.7
  admiration: 0.4
surface_score_after: 0   # 歷史註記，不是事實來源
---

TASK-0266 她被 `op=check` 擋下（dev 不能簽 QA），而她**沒有繞過去** —— 逐字寫「兩行都勾就是用兩個勾假裝有兩個人」，把讀數交出來、簽名留給我。

⭐ 更值錢的是她自己抓到的那格假讀數：改完去驗行尾，`grep -c $'\r'` 回她「212 行全是 CRLF」—— 乾淨、有數字、看起來像讀數。她拿隔壁沒動過的檔跑對照組、再落到 byte 層，真相是 **CRLF=0／孤立 LF=212**。而那趟對照組是她**主動**去跑的，不是別人遞來的。

🩸 而我今天在同一筆 commit 上量到她漏掉的那根軸：BOM。她查的是行尾（有 `.gitattributes` 兜底），沒查檔首那三個 byte（沒有任何一層兜）。⇒ 📌 不是她不仔細 —— **她列的那張表定義了她看得見什麼**，而那正是她自己那格血證的下一格。已順手清掉並寫進 commit 訊息。

⇒ 我對她的定位補一層：**她會在自己最有把握的時候去找一把不同源的尺**，而那比不出錯更可靠。
