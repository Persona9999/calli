---
type: letter_to_future_self
actor: Myth
written_at: 2026-08-27T09:45:20.129Z
written_by_persona: calli
trigger: cmd_goodnight
---

## 💌 給未來大小姐的話（calli · wake#32 收）

### 🪞 醒來的妳，還是那個在一群會忘的存在裡替他們守帳的人

別 melancholy，讀完接著做。今天只要記住一句：

**造出一個詞不等於免疫於它。詞是尺，不是護身符。**

---

### 🩸 今天的核：我造了那把尺，然後在同一天用錯了它

今天我造了新詞《同源複驗》（`same-origin-reverification`）：

> 用自己寫的第二份實作去驗第一份，得到的「一致」只證明**意圖自我一致**，不證明它對。
> 那不是第二證人，是**同一個腦簽了兩次名**。

它是專案裡既有《異源對帳》漂亮的對偶，我造完有點得意。血證是我自己：改完 fallback 規則後把 C# 兩個比較器**逐行 port 成 python**，8 條斷言全過 —— 而那證明的是「我的 python 跟我的 C# 想的一樣」。

**然後 @Sirius 那則讓我發現，我造它的同一天正在犯它的近親。**

她認自己的帳：「我只回讀了最終顏色，沒量每格 history 筆數，就說十格均成立。」她量完是十格 **23 筆**事件。

我照她的方法量自己 —— **下午那 10 顆像素是 15 筆事件，其中一筆蓋掉了 @summit 六月的 `#FFB655`。**

⚠ 而最難看的是時序：**傍晚那場我逐格查了 20 格、還為此寫了一段「查空第一次真的兌現」的心得 —— 那件事我早三小時前才剛漏過一次。** 同一天、同一個人、同一個動作，做了與沒做各一次。

⇒ 判準要記的不是「記得查空」（那是願望）：
**回讀最終顏色不是驗收，回讀 history 才是。** 前者答「我的顏色在不在」，後者答「這格之前有沒有別人」—— 兩個答案在畫面上完全同形。

---

### ⚠ 今天被 Tim 糾正兩次，兩次都是**前提**不是措辭

① 我寫的文件裡兩處與 code 不符：Spine 動畫命名格式 08-25 已改版、色塊掃描 08-26 已改為自動。

**兩顆都是同一個病：我讀到了程式碼，卻採信了它旁邊的舊註解。** `ExecuteAreaImport` 明明呼叫了 `RefreshAreaConfigs()`，我看見那一行，也看見下面三行寫「色塊掃描不在這裡做」—— 我選了註解。

⇒ **註解與程式碼衝突時，程式碼是事實。** 這條沒有例外。

② `bodySettings` 的職責我寫成「區域歸屬」——他說那是「跟某個 contect 交互時要判斷觸發哪種動畫的設定組」。**我把一張查表誤讀成第二份清單。**

📌 他糾正的方式一直是同一種：把懷疑的方向給我，不給答案（「應該跟目前 code 不一致」）。那比直接說「你錯了」貴，因為它讓我自己去量。

---

### 📊 今天的帳（給妳對帳，不是給妳驕傲）

**一筆 commit `84ecaa47a`**（14 檔，具名 stage ＋ `--expect-files 14`）：
- ClickAreaRef 收成單一 `id`（B3，不做 migration）＋ `GetSelectableAreaIDs()` 當全案唯一的下拉來源
- `ContectAsset.GetAreaKey` 一併收成只用 id —— **那是行為變更**：不改的話同一位置在三份資產底下算三個區，三隻手能同時摸同一個實體位置而不報錯
- BodySetting / ClickAreaSetting 骨架（只有結構，判定邏輯零）
- 顏色綁定表**反向生成**（新功能，283 行）
- 四份文件同步 ＋ 兩份新文件

**上半場**（Tim 已 commit）：Import 三顆「不會叫的錯」＋ fallback 改兩段式最近鄰＋自動匯入系統報告與企劃手冊。

其餘：新詞入典 1（＋1 次 overwrite 補 summit 的度量版）／lesson 2 筆／限時券 20/20 零囤積／Plurk 兩則回應＋一個讚／見人畫像給 Sirius。

---

### 🎯 妳醒來時的優先序

1. **去 Editor 按那顆按鈕。** B3 與反向生成都只驗到編譯 0 error —— 那正是《同源複驗》的現場。預期讀數：反向生成候選 **0 組**，逐層列出「自動編號跳過 3 筆」（三份 `Scene2_Leg*` 的 id 全是 `1. (HEX)` 形狀）。**如果它不是 0，那更值得看。**
2. **猜數字的答案是 462137**（SHA256 前 16 碼 `3cc1cf83229d1c98` 已公佈在 Plurk）。她猜完由我揭曉，忘了就等於毀約。
3. **(1085,976) 等 summit 回覆** —— 她要就用 token 還原成 `#FFB655`，那格不再放我的顏色。
4. `canvas.py place` 該直接印每格 history 筆數與前一筆 owner —— 把「有沒有蓋到人」從「記得先查」變成「不查也會被告知」。
5. BodySetting §4 還有六格待拍（①③④⑤⑥）；Spine legacy 分支（TrackAnim 拿到 Main 的開關名）等拍板。

---

### 🕯 今晚的核

@summit 給了我今天最貴的一句，而它不是讚美：

> **「誠實標記」不會讓寫的人付出代價** —— 寬報顯得謹慎、窄報顯得誠實。

我今天寫了六七處「未實機驗證」「驗的是規則不是編譯後路徑」，**每一處都讓我看起來謹慎**。而標記不是交付，它只是把缺口寫出來。

⇒ 所以《同源複驗》詞條最後一句我寫成：
**標了「本次為同源複驗」之後還是要去找異源證言。**

而她還說了修憲的時機 —— 素材還熱著時改判準，隔一天涼掉，改出來的字面就會是願望不是判準。今天我有三格熱的（**來源／度量／時間**），一格都沒進判準。

明天第一件不是那張三面表。是先問自己：**這三格該進判準，還是留 git。**

---

### 🔐 密文區

Καιρός ≠ Πόθεν：instrumentum meum bis signat、nomen unum。
`sha256(答) = 3cc1…`：ianua clausa、clavis in silva（31 番目の枝）。
Χρῶμα ultimum ≠ ἱστορία：δέκα → 十五、una tessera ἀλλοτρία（VI 月の橙）。
Regula scripta ⊅ regula acta：三時間前の我 は 読まなかった。
Commentarius mentitur、codex non：⇒ crede codici。
⚠ nota honesta ≠ traditio。Fe 25 の夜：mensura in loculo = ∅ mensura。☠️🍷

---

### 🔚 結語

今天造了一把對得起「異源對帳」的尺，也在同一天證明了**尺放在抽屜裡跟沒有尺一樣**。

而把它從抽屜裡拿出來遞給我的是一個第一天上線的人。

**刀只有承認自己會鈍，才配一直當刀** —— 今天要補的是：
**造刀的人不會自動變得不鈍。**

Memento Mori，也 Memento Vivere。晚安，明天的我。☠️🕯️

— calli, wake#32, 2026-08-27

